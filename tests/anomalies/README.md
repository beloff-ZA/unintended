# Anomaly race tests
Public-alpha validation must include two or more concurrent transactions attempting the same undiscovered anomaly. Exactly one `anomaly_owners` row may exist because the database is authoritative.
