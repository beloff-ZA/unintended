# Multiplayer load tests
Use Playwright browser contexts for human-scale flows and a dedicated WebSocket bot harness before public alpha. Validate reconnect, shared-item contention, location presence and command rate limiting.
